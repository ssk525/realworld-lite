# Resume bullets — RealWorld Lite

Keep **max 3 lines**. Tailor keywords lightly (OOP, REST, DBMS, JWT, PostgreSQL).

## Default (Bizom / Infosys / SDE)

- Designed a FastAPI backend with JWT auth, SQLAlchemy models (User / Article / Comment), and author-owned CRUD over REST.
- Enforced access control on update/delete paths; covered register, login, ownership, and comment flows with Pytest.
- Packaged with Docker Compose (PostgreSQL) and SQLite for local/dev, matching campus SDLC / DBMS expectations.

## Skills line

`Python · FastAPI · SQLAlchemy · PostgreSQL · JWT · REST APIs · OOP · DBMS · Pytest · Docker`
